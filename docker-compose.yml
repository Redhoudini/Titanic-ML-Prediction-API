#ANSVAR:
#  Starter to services: web (Flask) og db (Postgres).
#  Sætter ports, env-vars, volume (så kode ændringer sync’er), og “healthy” check.


services:
  web:
    build: .
    container_name: nn_web
    env_file: .env
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
    depends_on:
      db:
        condition: service_healthy
    command: python manage.py

  db:
    image: postgres:16
    container_name: nn_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 3s
      timeout: 3s
      retries: 20

volumes:
  pgdata: